using UnityEngine;
using System.Collections;

public class CollisionScripts : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
    //void Update () {
	
    //}

    //void OnTriggerEnter(Collider col)
    //{
    //    if(col.tag == this.tag)
    //    {
    //        col.transform.position = col.GetComponent<Enemy>().prevPos;
    //    }
    //}

    //void OnCollisionEnter(Collision col)
    //{
    //    if (col.collider.tag == this.tag)
    //    {
    //        col.transform.position = col.collider.GetComponent<Enemy>().prevPos;
    //    }
    //}
}
